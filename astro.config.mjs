import { defineConfig } from 'astro/config';

// IMPORTANT: replace with your own GitHub username / repo name once you
// create the repo, so links and assets resolve correctly on GitHub Pages.
// Example: if your repo is github.com/malaika/coherence-log, then
//   site: 'https://malaika.github.io'
//   base: '/coherence-log'
export default defineConfig({
  site: 'https://QuantTech-01.github.io',
  base: '/coherence-log',
});
